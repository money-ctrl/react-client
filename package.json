{
  "name": "ctrl-money",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ctrlmoney.web.app/",
  "scripts": {
    "start": "REACT_APP_VERSION=dev node scripts/start.js",
    "build": "REACT_APP_VERSION=$(git rev-parse --short HEAD) node scripts/build.js",
    "test": "node scripts/test.js",
    "predeploy": "npm run build",
    "deploy": "firebase deploy"
  }
}
